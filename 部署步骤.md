# A-tech 旅行管理系统 - 部署步骤

## 第一步：部署后端到 Railway

### 1. 注册/登录 Railway
- 访问：https://railway.app/
- 点击右上角 "Login" 或 "Start a New Project"
- 使用 GitHub 账号登录（或邮箱注册）

### 2. 创建新项目
- 登录后，点击 "New Project"
- 选择 "Deploy from GitHub repo"
- 如果是第一次，需要授权 Railway 访问你的 GitHub
- 在仓库列表中找到并选择 `atech-travel-system`

### 3. 配置后端服务
- Railway 会自动检测到项目
- 点击检测到的服务
- 进入 Settings（设置）
- 找到 "Root Directory"，设置为：`backend`
- 找到 "Start Command"，确认是：`npm start`

### 4. 添加环境变量
- 在项目页面，点击 "Variables" 标签
- 添加以下变量：
  - `NODE_ENV` = `production`
  - `PORT` = `5000`
  - `JWT_SECRET` = `your-secret-key-change-this-in-production`

### 5. 生成公开访问地址
- 点击 Settings
- 找到 "Networking" 部分
- 点击 "Generate Domain"
- **复制生成的域名**（例如：`https://atech-travel-system-production.up.railway.app`）
- 这个地址等会配置前端要用

### 6. 等待部署完成
- 查看 "Deployments" 标签，等待状态变为 "Success"
- 可以点击 "View Logs" 查看部署日志

---

## 第二步：部署前端到 Vercel

### 1. 注册/登录 Vercel
- 访问：https://vercel.com/
- 点击 "Sign Up" 或 "Login"
- 使用 GitHub 账号登录

### 2. 导入项目
- 点击 "Add New..." → "Project"
- 点击 "Import Git Repository"
- 找到并选择 `atech-travel-system` 仓库
- 点击 "Import"

### 3. 配置项目设置
**重要配置：**

- **Framework Preset**: 选择 `Create React App`
- **Root Directory**: 点击 "Edit"，选择 `frontend` 目录
- **Build Command**: `npm run build`（自动填充）
- **Output Directory**: `build`（自动填充）

### 4. 添加环境变量
在 "Environment Variables" 部分：
- 点击 "Add"
- Name: `REACT_APP_API_URL`
- Value: `你在 Railway 复制的域名/api`
  - 例如：`https://atech-travel-system-production.up.railway.app/api`
- 点击 "Add"

### 5. 部署
- 点击 "Deploy" 按钮
- 等待部署完成（通常 2-3 分钟）
- 部署成功后会显示访问地址（例如：`https://atech-travel-system.vercel.app`）

### 6. 测试系统
- 点击访问地址
- 使用演示账号登录：
  - 销售账号：`sales1` / `123456`
  - 运营账号：`ops1` / `123456`
  - 供应商账号：`supplier1` / `123456`

---

## 常见问题

### Railway 部署失败
- 检查 Root Directory 是否设置为 `backend`
- 查看 Logs 了解具体错误
- 确认 package.json 中有 `start` 脚本

### Vercel 部署失败
- 检查 Root Directory 是否设置为 `frontend`
- 确认环境变量 `REACT_APP_API_URL` 格式正确（必须以 `/api` 结尾）
- 查看 Build Logs 了解具体错误

### 前端无法连接后端
- 确认 Railway 后端已成功部署并生成了域名
- 确认 Vercel 环境变量中的 API 地址正确
- 在 Vercel 项目设置中重新部署（Deployments → 最新部署 → Redeploy）

### 数据丢失
- Railway 免费版重启后 JSON 数据库会重置
- 如需持久化，建议升级 Railway 计划或使用外部数据库

---

## 部署完成后

你会得到两个地址：
- **后端 API**：`https://xxx.railway.app`
- **前端网站**：`https://xxx.vercel.app`

把前端地址分享给其他人，他们就可以访问系统了！

---

## 更新代码

当你修改代码后：

1. 提交并推送到 GitHub：
```bash
cd /Users/Min/.claude/skills/frontend-design/atech-travel-system
git add .
git commit -m "更新说明"
git push
```

2. Railway 和 Vercel 会自动检测到更新并重新部署
3. 等待几分钟后刷新网页即可看到更新
